{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "STATIC_DEPLOY=true npm run build",
  "outputDirectory": "dist/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
